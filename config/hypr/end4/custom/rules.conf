# End-4 hybrid — custom/rules.conf — user overrides (not overwritten on update)
# Put your overrides here; this file is sourced after end4/rules.conf
# Example: env = MY_VAR,value
# Example: bindd = SUPER, X, My action, exec, myapp
