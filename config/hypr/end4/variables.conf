# End-4 hybrid — variables (from variables.lua)
# Original lua vars → Hypr $vars. Edit custom/variables.conf to override.
$qsConfig = caelestia
$terminal = foot
$fileManager = thunar
$browser = firefox
$codeEditor = code
$officeSoftware = libreoffice
$textEditor = kate
$volumeMixer = pavucontrol
$settingsApp = systemsettings
$taskManager = btop
# Workspace group size (End-4: 10)
# Used for Super+1..0 grouping via workspace_in_group() — implemented via workspace = m+1 / movetoworkspace in keybinds.conf
